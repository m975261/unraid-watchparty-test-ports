# Unraid Templates for watchparty-test-ports

XML templates for Unraid Community Applications.

## Install
Copy `my-watchparty-test-ports.xml` to `/boot/config/plugins/dockerMan/templates-user/` on Unraid,
then: Docker tab → Add Container → select from template list.
